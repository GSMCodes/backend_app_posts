<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-blue-800 py-8 text-white px-4">
        <a class="absolute right-0 mr-4 inline-block bg-red-600 rounded p-3 " href="/logout">Log Out </a>
        <h3 class="text-xl">Edit your post!</h3>
        <h5 class="text-lg mb-2 ">Go ahead and edit the post:</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea  name="content" placeholder="What's on your mind?" class=" block resize-none text-black px-4 py-2 w-1/3 rounded" > <%= post.content %> </textarea>
            <input class="px-4 py-3 bg-blue-500 block mt-2 rounded-md mb-10" type="submit" value="Update Post">
        </form>
        
                
    </div>
  </body>
</html>